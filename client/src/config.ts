export const API_MOCKS = true;

export const baseUrl = window.location.protocol + '//' + window.location.host;

export const couchUrl = baseUrl;
export const couchPrefix = '/couch';
//export const couchUrl = 'http://localhost';
//export const couchPrefix = ':5984';

export const questionIdLength = 12;